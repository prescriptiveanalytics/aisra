syntax = "proto3";

option csharp_namespace = "HEAL.HeuristicGrpc.Core.Proto";

message GrpcFuncProblem {
    string function = 1;
    int32 dimensions = 2;
    int32 population = 3;
    double mutation_rate = 4;
    double mutation_strength = 5;
    int32 max_iterations = 6;
}

message GrpcSolution {
    repeated double values = 1;
}

service GrpcHeuristicService {
    rpc RunBenchmark (GrpcFuncProblem) returns (GrpcSolution);
}
